# Краткое саммари приложения GigaChat Assistant

## 🎯 Назначение
Веб-приложение для общения с AI-моделями через удобный чат-интерфейс с поддержкой RAG (Retrieval-Augmented Generation) для контекстного поиска информации.

## 🏗️ Архитектура

### Основные компоненты

**Frontend:**
- Flask веб-сервер с Jinja2 шаблонами
- Современный адаптивный UI (HTML/CSS/JavaScript)
- Реал-тайм чат с историей сообщений

**Backend:**
- `chatbot_gui.py` - главный Flask-модуль, роутинг и обработка запросов
- `gigachat_client.py` - интеграция с GigaChat API, поддержка tools и RAG
- `vector_store.py` - работа с PostgreSQL/pgvector для векторного поиска
- `db_utils.py` - работа с SQLite для истории чатов и пресетов
- `chat_utils.py` - утилиты (токены, стоимость, валидация)
- `weather_tool.py` - пример интеграции внешних tools

### Хранилища данных

1. **SQLite** (`chat_history/chat_history.db`)
   - История диалогов (сессии и сообщения)
   - Предустановки системных промптов

2. **PostgreSQL + pgvector** (опционально, для RAG)
   - Векторная база знаний (`public.chunk_embeddings`)
   - Поиск релевантных чанков по эмбеддингам

## 🚀 Ключевые возможности

### 1. Мультипровайдерность
- **GigaChat**: GigaChat, GigaChat-Pro, GigaChat-Pro-Max
- **Hugging Face**: DeepSeek R1, Arch Router, L3-8B Stheno

### 2. RAG (Retrieval-Augmented Generation)
- Поиск релевантных чанков в векторной БД перед генерацией ответа
- Эмбеддинги через Ollama (nomic-embed-text)
- Автоматическое добавление контекста в промпт
- Отображение источников (path, headings, score) в ответе

### 3. Управление промптами
- Предустановки системных промптов
- Создание и сохранение кастомных промптов через UI
- Переключение между промптами в реальном времени

### 4. История и метаданные
- Сохранение всех диалогов в SQLite
- Просмотр истории сессий
- Отображение метаданных: токены, время ответа, стоимость (для GigaChat)

### 5. Интеграция Tools
- Поддержка function calling (weather tool как пример)
- Автоматическое определение необходимости вызова tools
- Обработка результатов tools и формирование финального ответа

## 📊 Технологический стек

**Backend:**
- Python 3.10+
- Flask 3.0+ (веб-фреймворк)
- GigaChat SDK (официальный SDK)
- Hugging Face Hub (Inference API)
- psycopg2-binary (PostgreSQL)
- python-dotenv (переменные окружения)

**Frontend:**
- HTML5, CSS3, JavaScript (vanilla)
- Современный адаптивный дизайн

**Базы данных:**
- SQLite (история, пресеты)
- PostgreSQL + pgvector (векторный поиск)

**Внешние сервисы:**
- GigaChat API
- Hugging Face Inference API
- Ollama (для эмбеддингов)

## 🔄 Поток работы RAG

1. Пользователь отправляет вопрос с включенным флагом RAG
2. Создается эмбеддинг запроса через Ollama API
3. Поиск top-k релевантных чанков в PostgreSQL (cosine similarity)
4. Формирование контекста из найденных чанков
5. Добавление контекста в системный промпт
6. Вызов GigaChat с расширенным промптом
7. Возврат ответа с метаданными об источниках

## 📁 Структура проекта

```
gigachat_api/
├── chatbot_gui.py          # Главный Flask-модуль
├── gigachat_client.py       # Клиент GigaChat с RAG
├── vector_store.py          # Работа с pgvector
├── db_utils.py             # SQLite утилиты
├── chat_utils.py           # Общие утилиты
├── weather_tool.py          # Пример tool
├── templates/              # HTML шаблоны
│   ├── index.html          # Главная страница чата
│   └── history.html        # История сессий
├── static/                 # Статические файлы
│   └── styles.css          # Стили
├── chat_history/           # SQLite БД
└── config/                 # Конфигурация
```

## ⚙️ Конфигурация

Переменные окружения (`.env`):
- `GIGACHAT_CREDENTIALS` - токен GigaChat
- `HUGGINGFACE_API_TOKEN` - токен HF (опционально)
- `DATABASE_URL` - PostgreSQL для RAG (опционально)
- `OLLAMA_URL` - URL Ollama для эмбеддингов
- `TOP_K` - количество релевантных чанков (по умолчанию 8)
- `PGVECTOR_METRIC` - метрика поиска (cosine/l2/inner_product)

## 🎨 Особенности UI

- Современный дизайн с градиентами и тенями
- Адаптивная верстка (мобильные устройства)
- Автоматическая прокрутка к новым сообщениям
- Подсчет токенов в реальном времени
- Модальные окна для сохранения пресетов
- Отображение метаданных и источников

## 🔐 Безопасность

- Секретные ключи через переменные окружения
- Flask session для состояния пользователя
- Валидация входных данных
- Обработка ошибок API

## 📈 Масштабируемость

- Модульная архитектура (легко добавлять новые провайдеры)
- Разделение ответственности (UI, логика, БД)
- Поддержка пула соединений (можно добавить)
- Оптимизация запросов к векторной БД (индексы, параметры)

## 🛠️ Расширяемость

Легко добавить:
- Новые провайдеры моделей
- Дополнительные tools
- Новые типы метаданных
- Альтернативные векторные БД
- Экспорт/импорт истории

---

**Версия:** 2.0 (с поддержкой RAG)  
**Статус:** Production-ready  
**Лицензия:** См. LICENSE файл

